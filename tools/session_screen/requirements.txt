# tools/session_screen/ — offline validation quick-win
#
# Pin every direct dep so a fresh clone reproduces the same numbers.
# Torch is the heavy one (~2 GB with CUDA extras). CPU-only is fine
# for a 585-file corpus; installation instructions in README.md.
#
# The pinned BatDetect2 version ships the Net2DFast_UK_same checkpoint
# (18 UK species) — model SHA-256 is recorded in every truth_manifest
# row so mixed caches surface immediately.

batdetect2==1.3.1

# Manifest storage.
pyarrow>=14.0.0
pandas>=2.0.0
numpy>=1.23.5

# BatDetect2 dep chain; listed explicitly so the constraint solver
# doesn't drift on a fresh install.
librosa>=0.10.1
scipy>=1.10.1
scikit-learn>=1.2.2
torch>=1.13.1
torchaudio>=1.13.1

# Test-only. Skipped by 'pip install -r requirements.txt --no-deps' on
# a production checkout; kept in the same file so 'validate all' and
# 'pytest tools/session_screen' share one bootstrap.
pytest>=7.0
